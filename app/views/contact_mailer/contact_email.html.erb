<!DOCTYPE html>
<html>
  <head></head>
  <body>
    <%# @name, @email, @body from app/mailers/contact_mailer.rb %>
    <p>You have received a message from the web developer course upskill, from <%= "#{@name}, #{@email}."%> </p>
    <p><%= @body %></p>
  </body>
</html>